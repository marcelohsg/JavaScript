let idade = "20"

if (idade == 20) {
    idade += 15
    console.log('idade adicionada com sucesso!')
    console.log(idade)
}

// == É um mais flexível, deixando passar pela validação e concatenando com o 15 ao invés de somar.
// === É mais rigido, fazendo com que apenas uma variável number seja valida para ocorrer a operação.